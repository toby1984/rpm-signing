# Stuff that might (or might not) be useful

- Add support for GZIP compression to both server and client (to cut down on transmission times and data volume)
- Add support for signing arbitrary data and returning an ASCII-armored, detached GPG signature to both client and server
